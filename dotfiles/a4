#!/usr/bin/env bash
set -e

# Change to the praxis project directory and use devenv to run cargo
cd "$HOME/Documents/praxis"
exec devenv shell -- cargo run --quiet --manifest-path core/aethel/Cargo.toml -- "$@"